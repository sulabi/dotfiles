local comment = {
	"numToStr/Comment.nvim",

	opts = {
		toggler = {
			line = "<leader>/",
			block = "<leader>;"
		},

		opleader = {
			line = "<leader>/",
			block = "<leader>;"
		}
	}
}

return comment
